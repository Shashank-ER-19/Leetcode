Leetcode 2239
Find closest nu ber to zero


initialise a var to first element of list
loop over rem elem-->x:
  check if abs(x)<abs(closest):closest=x
check if closest<0 and abs(closest) in list:
    return abs(closest)
return closest
